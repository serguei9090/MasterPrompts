#pre-commit:
#  parallel: true
#  commands:
#    lint-check:
#      run: uv run ruff check {staged_files} && bunx biome check --apply --files-ignore-unknown=true {staged_files}
#      glob: "*.{py,js,ts,tsx,json,css}"
#      skip:
#        - merge
#        - rebase

post-commit:
  commands:
    codanna-index:
      # Use git diff-tree to get files from the commit just created
      run: uv run python scripts/codanna/index.py $(git diff-tree --no-commit-id --name-only -r HEAD)
    cognee-index:
      run: uv run python scripts/cognee/indexer.py $(git diff-tree --no-commit-id --name-only -r HEAD)
