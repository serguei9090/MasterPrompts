# Spec: MEM_001 - AI Memory Management System

## 🎯 Objective
Establish a persistent, high-signal memory management system to optimize AI context retrieval, architectural continuity, and cross-session knowledge retention.

## 🏗️ Architecture
- **Storage Layer**: `docs/memory/` (Optimized Markdown Hierarchy)
- **Governance Layer**: `@memory-manager` (Specialized Persona)
- **Interface Layer**: `memory` (Skill) & `/memory` (Gemini Command)
- **Operational Layer**: Workflows for CRUD operations and context distillation.
- **Thinking Layer**: Integration of `sequentialthinking` MCP into core logic.

## 📅 Roadmap

### Phase 1: Foundation (Structure & Tracker)
- [ ] Create `docs/memory/` directory structure.
- [ ] Initialize `docs/memory/README.md` (System Overview).
- [ ] Initialize `docs/memory/index.md` (Global Memory Map).
- [ ] Register `MEM_001` in `docs/track/TODO.md`.

### Phase 2: Persona & Rule Governance
- [ ] Create `.gemini/agents/memory-manager.md`.
- [ ] Add `MemoryManagement.md` rule to `.agents/rules/System/`.
- [ ] Update `AGENTS.md` to include `@memory-manager`.

### Phase 3: Capabilities (Skill & Command)
- [ ] Create `memory` skill using `skill-creator`.
- [ ] Implement `gemini/memory.toml` command.

### Phase 4: Operations (Workflows)
- [ ] Create `/CreateMemory` workflow.
- [ ] Create `/UpdateMemory` workflow.
- [ ] Create `/DeleteMemory` workflow.
- [ ] Create `/MemoryAudit` workflow.

### Phase 5: Smith Integration & Sequential Thinking
- [ ] Update `smith_orchestra_auto.md` with memory-sync and sequential-thinking steps.
- [ ] Update `smith_orchestra_manual.md` with memory-sync and sequential-thinking steps.
- [ ] Implement a "Backup" Sequential Thinking workflow (for cases where MCP tool is missing).

## 🛠️ Tech Stack
- **Format**: Markdown (GitHub Flavored)
- **Orchestration**: Google ADK 2.0
- **Verification**: `sequentialthinking` MCP
